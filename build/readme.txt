1、查询节点的数据逻辑修改，一般查询节点将查询结果添加到第0个数据项中，关联查询可基于多个数据项做查询  完成
2、增加数字类型数据分组节点   完成
3、增加数字类型字段分组校验节点 完成
4、增加整体校验节点，可校验之前某一个分组校验是否都成功  场景不明确暂时不做
5、增加延时等待节点   完成
6、比对节点支持2个以上表的比对，左右表可分别对应多个表的数据   完成
7、比对节点中的容差优化，支持左右两个容差值 完成

8、增加比对结果处理节点
    1、在比对分组基础上的核销，需要更新分组上的核销状态，另外在原始数据上的也需要更新核销状态，核销金额，剩余金额，核销数量，剩余数量
    2、考虑对于导入的原始数据字段保持不变，原始数据中仅保留最后一次的状态，待核销金额，待核销数量，已经比对金额，已经比对数量
    3、对于每个参与比对的数据，同时增加一个比对核销记录表，从原始数据中获取参与比对的关键字段放入比对记录表中
    4、对于每个原始数据可以查看具体的核销记录明细，原始数据上的核销和比对状态需要和比对记录表的最后一条记录的状态保持一致
    5、

9、配置页面节点和流增加备注信息
10、配置属性的名称栏宽度可修改
11、部分配置项目设置方式优化
12、调试节点增加选项参数，可以控制输出内容（分组数量，分模型的记录汇总数，基于某个校验结果统计分组数）
